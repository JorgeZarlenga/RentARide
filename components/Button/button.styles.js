import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderRadius: 8,
    backgroundColor: 'red',
    width: 120,
    height: 120,
  },
});

export default styles;
