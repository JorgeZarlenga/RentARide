import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginTop: 24,
    borderRadius: 20,
    backgroundColor: '#628DC2',
    width: 240,
    height: 40,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonText: {
    color: '#FFF',
    fontWeight: '25',
  },
});

export default styles;
